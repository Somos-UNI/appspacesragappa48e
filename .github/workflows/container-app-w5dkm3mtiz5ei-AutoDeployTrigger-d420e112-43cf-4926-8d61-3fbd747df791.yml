name: Trigger auto deployment for container-app-w5dkm3mtiz5ei

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-w5dkm3mtiz5ei-AutoDeployTrigger-d420e112-43cf-4926-8d61-3fbd747df791.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPW5DKM3MTIZ5EI_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPW5DKM3MTIZ5EI_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPW5DKM3MTIZ5EI_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPW5DKM3MTIZ5EI_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPW5DKM3MTIZ5EI_REGISTRY_PASSWORD }}
          containerAppName: container-app-w5dkm3mtiz5ei
          resourceGroup: appspacesragappa48e8d1d
          imageToBuild: default/container-app-w5dkm3mtiz5ei:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


